#ifndef GAUSSIAN_RANDOM_NUMBERS_H
#define GAUSSIAN_RANDOM_NUMBERS_H

#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <time.h>

double* gaussian_random_numbers(int no_of_iterations);

float* gaussian_random_numbers1(int no_of_iterations);

#endif
